services:
  gdc:
    image: node:21-alpine
    working_dir: /app
    volumes:
      - ./interfacekeycloak:/app
    command: sh -c "cd backend && npm install && npm run dev & cd frontend && npm install && npm run dev -- --host 0.0.0.0"
    ports:
      - "9500:3333"
      - "9510:5173"
